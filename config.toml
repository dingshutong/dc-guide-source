baseURL = "https://dingshutong.github.io/dc-guide/"
languageCode = "en-us"
title = "DSD Constructor User Guide"

# Change the default theme to be use when building the site with Hugo
theme = "hugo-theme-learn"

# For search functionality
[outputs]
home = [ "HTML", "RSS", "JSON"]